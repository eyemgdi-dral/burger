import css from "./style.module.css";
export const Toolbar = () => {
  return (
    <header className={css.Toolbar}>
      <div>...</div>
      <div>Logo</div>
      <nav>Menu</nav>
    </header>
  );
};
